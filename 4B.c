// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,m,sum=0;
    scanf("%d %d", &n, &m);
    if(n>m) printf("District-1 is in the Red Zone\n");
    else printf("District-2 is in the Red Zone\n");
    return 0;
}
